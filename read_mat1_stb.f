	subroutine read_mat1(isize_sigpair,name$,signal,
     $    outshort,outunit,ipres,
     $    ref_volt,equiv_db,equiv_press,dcoffset,
     $    srate,ierr)

	real signal(*), srate
	integer*2 isignal(1)
	character*(*) name$

	return
	end

